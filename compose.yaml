services:
  flask:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - koekake_data:/data
      - ./app:/app
    environment:
      KOEKAKE_DB: /data/koekake.db
      TZ: Asia/Tokyo

volumes:
  koekake_data:
